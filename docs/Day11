# 📅 Day 11 압축 관리(Tar) & 트러블슈팅 - 2026.01.16

## 🏷️ Category
> **주제:** Linux 압축 관리 (Tar) & Error Handling
> **태그:** #Linux #Tar #Compression #Troubleshooting #Shell

## 💡 Key Takeaways (핵심 요약)
리눅스 표준 압축 포맷인 `.tar.gz`를 다루는 방법과, 실습 중 발생한 경로/옵션 에러를 해결하며 배운 점들.

1. **Tar 압축/해제 명령어**:
   - `tar`는 파일을 묶는 도구이고, `gz`는 용량을 줄이는 압축 방식이다.
   - **압축하기(Create):** `tar -czf [만들파일.tar.gz] [대상폴더]`
   - **풀기(Extract):** `tar -xzvf [파일.tar.gz]`
   - **다른 곳에 풀기:** `-C` 옵션을 사용하여 목적지를 지정한다.

2. **옵션의 의미**:
   - `-c`: Create (생성)
   - `-x`: Extract (해제)
   - `-z`: gzip (압축 알고리즘)
   - `-v`: verbose (과정 출력)
   - `-f`: file (파일 지정, **항상 맨 뒤**)

3. **트러블 슈팅 & 깨달음**:
   - **덮어쓰기:** 이미 파일이 있어도 경고 없이 덮어쓴다.
   - **폴더 구조:** 폴더째로 압축하면, 풀 때도 폴더째로 나온다 (내용물이 흩어지지 않음).
   - **에러 해결:** "No such file"은 오타나 경로 문제다. **Tab 키** 자동완성으로 해결하자.

-------------------------------------------------------------------------------------

## 💻 Commands & Usage (실습 로그)

### 1. 압축 파일 생성 및 해제 실습
치과 데이터를 백업하고 복구하는 과정.

```bash
# 1. 압축하기 (Create)
# 문법: tar -czf [파일명] [대상]
$ tar -czf dental_backup.tar.gz 01_Dental_clinic
# (아무 메시지 없이 프롬프트가 뜨면 성공)

# 2. 압축 풀기 (Extract)
# 문법: tar -xzvf [파일명]
$ tar -xzvf dental_backup.tar.gz
# 결과: 01_Dental_clinic/ 폴더가 생성되며 그 안에 파일들이 복구됨

# 3. 다른 위치에 풀기 (-C 옵션)
$ tar -xzvf dental_backup.tar.gz -C ~/02_practice 




-------------------------------------------------------
  트러블슈팅 -옵션누락과 경로문제로 인한 에러 해결 로그.

# [Case 1] 옵션 누락 에러
$ tar dental_backup.tar.gz 01_Dental_clinic
# 에러: tar: Old option 'g' requires an argument.
# 해결: 동작 옵션(-czf)을 명시해야 함 -> tar -czf ...

# [Case 2] 파일 없음 에러
$ tar -xzvf backup.tar.zp
# 에러: Cannot open: No such file or directory
# 해결: 1. ls로 파일 확인 / 2. 정확한 확장자(.tar.gz) 입력 / 3. Tab 키 사용
