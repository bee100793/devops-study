### 📅 Day 15: AWS EC2 생성 및 SSH 트러블 슈팅 (26.01.27)

**목표:** AWS 클라우드 상에 Ubuntu 리눅스 서버(EC2)를 생성하고, 내 로컬 환경(WSL)에서 SSH로 원격 접속에 성공한다.

#### 1. EC2 인스턴스 생성
* **AMI:** Ubuntu Server 24.04 LTS (안정성 및 호환성 고려)
* **Instance Type:** t3.micro (Free Tier, Seoul Region 지원)
* **Key Pair:** `my_key.pem` (RSA 방식)

#### 2. SSH 접속 간 트러블 슈팅 (WSL 환경)
윈도우 파일 시스템(`/mnt/c/...`)에서 직접 접속 시도 시, 리눅스 권한 설정(`chmod`)이 적용되지 않아 `Unprotected Private Key File` 에러가 발생함.

* **원인:** NTFS(윈도우) 파일 시스템은 리눅스의 400(읽기 전용) 권한을 완벽하게 지원하지 않음.
* **해결:** 키 파일을 WSL의 리눅스 홈 디렉토리(`~`)로 복사하여 권한을 재설정함.

**[해결 명령어]**
```bash
# 1. 윈도우 다운로드 폴더에서 리눅스 홈으로 열쇠 복사
cp /mnt/c/Users/[User]/Downloads/my_key.pem ~

# 2. 홈으로 이동 후 권한 설정 (나만 읽을 수 있게)
cd ~
chmod 400 my_key.pem

# 3. 접속 성공
ssh -i my_key.pem ubuntu@[Public_IP]



